<%= form_for @conn, Routes.lobby_path(@conn, :create), fn f -> %>
  <label>
    Name: <%= text_input f, :name, required: true %>
  </label>

  <%= hidden_input f, :session_id, val: @conn.assigns[:session_id] %>

  <%= submit "Join game!" %>
<% end %>